load("@rules_cc//cc:defs.bzl", "cc_test")

config_setting(
    name = "cuda_build",
    define_values = {"cuda": "1"},
)

cc_test(
    name = "gaussian_2d_test",
    srcs = ["gaussian_2d_test.cc"],
    deps = ["//src/tinysplat"],
)

cc_test(
    name = "gaussian_2d_cuda_test",
    srcs = ["gaussian_2d_cuda_test.cc"],
    copts = select({
        ":cuda_build": ["-DTINYSPLAT_CUDA"],
        "//conditions:default": [],
    }),
    deps = ["//src/tinysplat"],
    tags = ["cuda"],
)
